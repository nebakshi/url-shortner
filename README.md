# URL Shortener
A simple URL shortener API built with Node.js and SQLite.
## Installation
1. Clone the repository: `git clone https://github.com/user/url-shortner.git`
2. Install dependencies: `npm install`
3. Start the server: `npm start`